#pragma once

#include "../Globals.h"

namespace demorender
{
	class CubemapRenderer
	{

	public:

		CubemapRenderer();
		~CubemapRenderer();

		/*
			for (all sides)
				set rendering to FBO 
				setup side projection etc
				render geometry

		
		
		*/

	private:

	};

}